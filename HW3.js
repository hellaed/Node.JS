//Heraldo Ghattas
//Yousef Hatukai

const arr = [0,1,0,2,0,3,0,4]
let zeroCounter = 0;
for(i=0;i<arr.length;i++)
{
  arr[i] == 0 ? zeroCounter++ : zeroCounter = zeroCounter
}
console.log(zeroCounter);